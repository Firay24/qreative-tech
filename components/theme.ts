import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    primary: {
      yellow: "#FFB733",
      dark: "#1E1E1E",
      light: "#FFEDCD",
      gray: "#696969",
      grayDark: "#5B5B5B",
    },
  },
});

export default theme;
