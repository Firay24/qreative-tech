import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    primary: {
      yellow: "#FFB733",
      dark: "#1E1E1E",
      light: "#FFEDCD",
    },
  },
});

export default theme;
